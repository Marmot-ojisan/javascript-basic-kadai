// 変数の宣言
let bmi;      // BMI
let taiju;    // 体重
let sincho;   // 身長

// 変数への代入
taiju = 68;   // 体重
sincho = 1.7; // 身長



// BMIの計算
bmi = taiju / (sincho * sincho);

// コンソールへの出力
console.log(bmi);
